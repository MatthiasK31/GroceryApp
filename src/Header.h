#include <string>

#pragma once
std::string askAndPrintName();

bool checkValidLetterInput(char, std::string);
bool checkValidNumInput(std::string);
void printItems(std::string, std::vector<std::string>, std::vector<double>);
